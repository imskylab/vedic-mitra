# Release signing configuration — TEMPLATE.
#
# Copy this file to `keystore.properties` (which is gitignored) and fill in your real values.
# NEVER commit the real keystore.properties or the .jks/.keystore file — they are your signing
# secrets. See docs/RELEASING.md for how to generate the keystore.
#
# storeFile is resolved relative to the repository root.
storeFile=keystore/release.jks
storePassword=CHANGE_ME
keyAlias=vedicmitra
keyPassword=CHANGE_ME
