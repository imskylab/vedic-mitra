<?xml version="1.0" encoding="utf-8"?>
<!--
  Vedic Mitra application manifest.
  Permissions required by future features (exact alarms, notifications, location) will be added
  in the phases that implement them; Phase 1 declares only the application shell.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".VedicMitraApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VedicMitra">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VedicMitra">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
