<?xml version="1.0" encoding="utf-8"?>
<!-- Feature module manifest. Namespace is declared in build.gradle.kts.
     RECEIVE_BOOT_COMPLETED lets the app re-arm reminders after a reboot (which clears alarms); the
     BootReceiver also re-arms them after the app is updated/reinstalled (MY_PACKAGE_REPLACED, which
     likewise cancels pending alarms). It is exported because the sender is the system, and both
     BOOT_COMPLETED and MY_PACKAGE_REPLACED are protected broadcasts only the system can send. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
